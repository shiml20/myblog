---
interface Props {
	date: Date;
	locale?: string;
}

const { date, locale = 'zh-CN' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale, {
			year: 'numeric',
			month: '2-digit',
			day: '2-digit',
		})
	}
</time>
